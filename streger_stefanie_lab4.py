speed = 12
if speed <=0 and speed <= 200: #Validates if variable between 0 and 200
    print("The number is valid")
if speed > 0 or speed > 200: #Validates if variable outside of the 0 to 200 range
    print("The number is not valid")
